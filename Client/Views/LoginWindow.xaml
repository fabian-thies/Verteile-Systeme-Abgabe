<Window x:Class="Client.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}"
        FontFamily="Segoe UI"
        WindowStyle="None"
        AllowsTransparency="True">
    <!-- Border for the modern card look -->
    <Border Background="White" CornerRadius="8" Padding="0"
            Effect="{DynamicResource DropShadowEffect}"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/> <!-- Custom title bar -->
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Custom Title Bar -->
            <DockPanel Grid.Row="0" Background="{StaticResource PrimaryBrush}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <TextBlock Text="Login" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="White"/>
                <Button Content="X" Width="40" Height="40" DockPanel.Dock="Right" 
                        Click="CloseButton_Click"
                        Background="Transparent" BorderBrush="Transparent" 
                        Foreground="White" FontWeight="Bold"/>
            </DockPanel>
            <!-- Content Area -->
            <StackPanel Grid.Row="1" Margin="20">
                <TextBlock Name="ErrorTextBlock" Foreground="Red" Text="" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                <TextBlock Text="Username:" Margin="0,0,0,5"/>
                <TextBox Name="UsernameTextBox" Margin="0,0,0,10"/>
                <TextBlock Text="Password:" Margin="0,0,0,5"/>
                <PasswordBox Name="PasswordBox" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Login" Click="LoginButton_Click" Margin="5"/>
                    <Button Content="Register" Click="RegisterButton_Click" Margin="5"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
