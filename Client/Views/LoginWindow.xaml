<Window x:Class="Client.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}"
        FontFamily="Segoe UI"
        WindowStyle="None"
        AllowsTransparency="True">
    <!-- English comment: Use a Border with a drop shadow effect for a modern card look -->
    <Border Background="White" CornerRadius="8" Padding="20"
            Effect="{DynamicResource DropShadowEffect}">
        <Grid>
            <StackPanel>
                <TextBlock Name="ErrorTextBlock" Foreground="Red" Text="" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                <TextBlock Text="Username:" Margin="0,0,0,5"/>
                <TextBox Name="UsernameTextBox" Margin="0,0,0,10"/>
                <TextBlock Text="Password:" Margin="0,0,0,5"/>
                <PasswordBox Name="PasswordBox" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Login" Click="LoginButton_Click"/>
                    <Button Content="Register" Click="RegisterButton_Click"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>