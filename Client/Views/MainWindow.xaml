<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Client.Views"
        Title="Chat Application" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}"
        FontFamily="Segoe UI"
        WindowStyle="None"
        AllowsTransparency="True">
    <Border Background="White" CornerRadius="8" Padding="0" Effect="{DynamicResource DropShadowEffect}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="{StaticResource PrimaryBrush}"
                  MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Chat Application" Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0"
                           FontWeight="Bold" FontSize="16" />
                <Button Content="X" Width="40" Height="40" Grid.Column="1" Click="CloseButton_Click"
                        Background="Transparent" BorderBrush="Transparent" Foreground="White" FontWeight="Bold"
                        VerticalAlignment="Center" />
            </Grid>
            <Grid Grid.Row="1">
                <TabControl Name="ChatTabControl" Margin="10">
                    <TabItem Header="Private Chat">
                        <Grid Margin="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
                                <TextBlock Text="To (Username):" VerticalAlignment="Center" />
                                <TextBox Name="PrivateTargetTextBox" Width="200" Margin="10,0" TextChanged="PrivateTargetTextBox_TextChanged" />
                            </StackPanel>
                            <ListBox Name="PrivateChatListBox" Grid.Row="1" ItemContainerStyle="{StaticResource ChatMessageStyle}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource ChatBubbleBackground}" CornerRadius="8" Padding="8" Margin="4">
                                            <TextBlock Text="{Binding}" TextWrapping="Wrap" />
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,10,0,0">
                                <TextBox Name="PrivateMessageTextBox" Width="500" KeyDown="PrivateMessageTextBox_KeyDown" />
                                <Button Content="Send" Click="SendPrivateMessageButton_Click" Margin="10,0" />
                                <Button Content="Whiteboard" Click="OpenPrivateWhiteboardButton_Click" Margin="10,0" />
                            </StackPanel>
                        </Grid>
                    </TabItem>
                    <TabItem Header="Group Chat">
                        <Grid Margin="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" /> <!-- Open groups list -->
                                <RowDefinition Height="Auto" /> <!-- Join/Leave controls -->
                                <RowDefinition Height="*" />    <!-- Chat messages -->
                                <RowDefinition Height="Auto" /> <!-- Message input -->
                            </Grid.RowDefinitions>

                            <GroupBox Header="Open Groups" Grid.Row="0" Margin="0,0,0,10">
                                <ListBox Name="OpenGroupsListBox" MouseDoubleClick="OpenGroupsListBox_MouseDoubleClick" />
                            </GroupBox>

                            <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
                                <TextBlock Text="Group Name:" VerticalAlignment="Center" />
                                <TextBox Name="GroupNameTextBox" Width="200" Margin="10,0" />
                                <Button Content="Join Group" Click="JoinGroupButton_Click" Margin="10,0" />
                                <Button Content="Leave Group" Click="LeaveGroupButton_Click" Margin="10,0" />
                            </StackPanel>

                            <ListBox Name="GroupChatListBox" Grid.Row="2"
                                     ItemContainerStyle="{StaticResource ChatMessageStyle}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource ChatBubbleBackground}" CornerRadius="8"
                                                Padding="8" Margin="4">
                                            <TextBlock Text="{Binding}" TextWrapping="Wrap" />
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,10,0,0">
                                <TextBox Name="GroupMessageTextBox" Width="500" KeyDown="GroupMessageTextBox_KeyDown" />
                                <Button Content="Send" Click="SendGroupMessageButton_Click" Margin="10,0" />
                                <Button Content="Whiteboard" Click="OpenGroupWhiteboardButton_Click" Margin="10,0" />
                            </StackPanel>
                        </Grid>
                    </TabItem>
                    <TabItem Header="Plugins">
                        <views:PluginManager />
                    </TabItem>
                    <TabItem Header="Files">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10">
                            <StackPanel>
                                <GroupBox Header="Upload File" Margin="5">
                                    <StackPanel Orientation="Horizontal" Margin="5">
                                        <Button Content="Upload File" Click="UploadFileButton_Click" Margin="5" />
                                        <TextBlock Name="UploadStatusTextBlock" VerticalAlignment="Center" Margin="5" />
                                    </StackPanel>
                                </GroupBox>

                                <GroupBox Header="Download File" Margin="5">
                                    <StackPanel Margin="5">
                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <TextBlock Text="Enter Document ID:" VerticalAlignment="Center" Margin="5" />
                                            <TextBox Name="DocumentIdTextBox" Width="200" Margin="5" />
                                        </StackPanel>
                                        <Button Content="Download File" Click="DownloadFileButton_Click" Margin="5" />
                                        <TextBlock Name="DownloadStatusTextBlock" VerticalAlignment="Center" Margin="5" />
                                    </StackPanel>
                                </GroupBox>

                                <GroupBox Header="File Versions" Margin="5">
                                    <StackPanel Margin="5">
                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <TextBlock Text="File ID:" VerticalAlignment="Center" Margin="5" />
                                            <TextBox Name="FileIdForVersionTextBox" Width="200" Margin="5" />
                                            <Button Content="Load Versions" Click="LoadVersionsButton_Click" Margin="5" />
                                        </StackPanel>
                                        <ListBox Name="FileVersionsListBox" Margin="5" Height="100" />
                                    </StackPanel>
                                </GroupBox>
                                <GroupBox Header="All Files" Margin="5">
                                    <StackPanel Margin="5">
                                        <Button Content="Load All Files" Click="LoadAllFilesButton_Click" Margin="5"
                                                HorizontalAlignment="Left" />
                                        <ListBox Name="AllFilesListBox" Margin="5" Height="150" />
                                    </StackPanel>
                                </GroupBox>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>
            </Grid>
        </Grid>
    </Border>
</Window>